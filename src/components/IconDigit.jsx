import React from "react";

const IconDigit = ({ digit }) => {
  return (
    <div className="text-black text-2xl flex flex-row items-center justify-center">
      <h1 className="text-4xl font-bold text-center ">{digit}</h1>
    </div>
  );
};

export default IconDigit;
